<h1>My Tasks:</h1>
<% @tasks.each do |task|%>
<p><%= link_to task.title, task_path(task) %> ( <%= link_to 'Delete', task_path(task), method: :delete, data: { confirm: "Are you sure?" }  %>
)</p>
<% end %>
<hr>
<%= link_to 'Add a new task', new_task_path(@task) %>
